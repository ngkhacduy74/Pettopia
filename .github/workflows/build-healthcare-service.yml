name: Build and Deploy Healthcare Service

on:
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - "backend/apps/healthcare-service/**"

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Login to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 3. Build & Push Docker Image
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend/apps/healthcare-service
          file: ./backend/apps/healthcare-service/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/pettopia-healthcare-service:latest

      # 4. Deploy via SSH to VPS
      - name: Deploy Healthcare Service on VPS
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            echo "➡ Pulling latest healthcare-service image..."
            cd /var/www/pettopia/Pettopia/backend
            docker compose pull healthcare-service

            echo "➡ Restarting healthcare-service..."
            docker compose up -d healthcare-service

            echo "➡ Cleaning old Docker images..."
            docker image prune -f

            echo "✔ Healthcare-service deployed successfully!"
